<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Melmo DCool Blogerк for Test</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
  <header class="bg-blue-600 text-white shadow-lg">
    <div class="max-w-4xl mx-auto px-6 py-6 flex justify-between items-center">
      <h1 class="text-2xl font-bold">🚀 My Cool Blog Sanjar</h1>
      <a href="/new" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-blue-100 transition">+ New Post</a>
    </div>
  </header>

  <main class="max-w-4xl mx-auto p-6 space-y-6">
    <% posts.forEach(post => { %>
      <div class="bg-white rounded-lg shadow p-6 hover:shadow-xl transition">
        <h2 class="text-2xl font-semibold text-blue-600"><a href="/post/<%= post.id %>"><%= post.title %></a></h2>
        <p class="mt-3 text-gray-700"><%= post.content.substring(0, 200) %>...</p>
      </div>
    <% }) %>
  </main>
</body>
</html>
